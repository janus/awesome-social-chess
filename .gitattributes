# Set the default behavior to automatically normalize line endings.
* text=auto

# Specify text files with CRLF line endings to be normalized to LF in the repository
*.bat text eol=crlf
*.ps1 text eol=crlf
*.sh text eol=lf

# Other specific text files can be added here as needed
